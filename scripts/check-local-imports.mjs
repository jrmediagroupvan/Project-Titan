import fs from "node:fs";import path from "node:path";const roots=["app","components","lib"];const exts=["",".ts",".tsx",".js",".jsx",".mjs","/index.ts","/index.tsx","/index.js"];let missing=[];function walk(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){const f=path.join(dir,e.name);if(e.isDirectory())walk(f);else if(/\.(ts|tsx|js|jsx|mjs)$/.test(e.name)){const t=fs.readFileSync(f,"utf8");for(const m of t.matchAll(/(?:from\s+|import\s*\()?["'](@\/[^"']+|\.{1,2}\/[^"']+)["']/g)){let rel=m[1];let base=rel.startsWith("@/")?path.resolve(rel.slice(2)):path.resolve(path.dirname(f),rel);if(!exts.some(x=>fs.existsSync(base+x)))missing.push(`${f}: ${rel}`);}}}}for(const r of roots)if(fs.existsSync(r))walk(r);if(missing.length){console.error("Missing local imports:\n"+missing.join("\n"));process.exit(1)}console.log("Local import checks passed.");
