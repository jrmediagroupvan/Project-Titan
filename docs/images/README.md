# Project images

Keep the GitHub banner at:

```text
docs/images/Project-Titan.png
```

The root `README.md` intentionally references that exact case-sensitive path.
Do not replace the real banner with a screenshot containing customer data,
credentials, API keys, or private server information.

