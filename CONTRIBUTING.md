# Contributing

- Branch from `develop`.
- Use `feature/<name>`, `fix/<name>`, or `release/<version>`.
- Never commit `.env`, backups, uploads, generated invoices, or customer files.
- Add tests for business logic and state transitions.
- Require review before merging into `main`.
