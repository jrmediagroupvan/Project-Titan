import { requireUser } from "@/lib/authorization";
import { updateProfileAction, changeOwnPasswordAction } from "@/app/actions";
export const dynamic="force-dynamic";
export default async function ProfilePage(){const u=await requireUser();return <><div className="top"><div><h1>My Profile</h1><p className="muted">Update your profile and password.</p></div></div><section className="card section"><h2>Profile</h2><form action={updateProfileAction} className="formGrid"><input name="name" defaultValue={u.name} required/><input value={u.email} disabled/><input name="jobTitle" defaultValue={u.jobTitle||""} placeholder="Job title"/><input name="phone" defaultValue={u.phone||""} placeholder="Phone"/><input name="avatarUrl" defaultValue={u.avatarUrl||""} placeholder="Profile image URL"/><button>Save profile</button></form></section><section className="card section"><h2>Change password</h2>{u.mustChangePassword?<p className="warning">You must change the temporary password before continuing.</p>:null}<form action={changeOwnPasswordAction} className="formGrid"><input name="currentPassword" type="password" placeholder="Current password" required/><input name="newPassword" type="password" minLength={8} placeholder="New password" required/><input name="confirmPassword" type="password" minLength={8} placeholder="Confirm new password" required/><button>Change password</button></form></section></>}
