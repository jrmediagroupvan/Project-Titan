# Testing Strategy

- `unit/` — pricing, tax, permissions, parsing, state transitions.
- `integration/` — database, object storage, provider sandboxes, webhook verification.
- `e2e/` — complete browser workflows for administrators and customers.
- `fixtures/` — safe test STL/3MF files and provider payloads.
