import {db} from "@/lib/db";import {requireUser} from "@/lib/authorization";import {markNotificationReadAction,createSystemNotificationAction} from "../advanced-actions";
export default async function Page(){const user=await requireUser();const [items,users]=await Promise.all([db.titanNotification.findMany({where:{OR:[{userId:null},{userId:user.id}]},orderBy:{createdAt:"desc"},take:100}),db.user.findMany({where:{active:true},orderBy:{name:"asc"}})]);return <><h1>Notification Center</h1><div className="two"><section className="card"><h2>Your notifications</h2>{items.map(n=><article className="record" key={n.id}><b>{n.title}</b><p>{n.message}</p><p className="muted">{n.severity} · {n.createdAt.toLocaleString()} · {n.readAt?"Read":"Unread"}</p>{!n.readAt&&<form action={markNotificationReadAction}><input type="hidden" name="id" value={n.id}/><button className="secondary small">Mark read</button></form>}</article>)}</section>{["OWNER","ADMIN"].includes(user.role)&&<form action={createSystemNotificationAction} className="card form"><h2>Send notification</h2><select name="userId"><option value="">Everyone</option>{users.map(u=><option key={u.id} value={u.id}>{u.name}</option>)}</select><input name="title" placeholder="Title" required/><textarea name="message" placeholder="Message" required/><select name="severity"><option>INFO</option><option>SUCCESS</option><option>WARNING</option><option>CRITICAL</option></select><input name="linkUrl" placeholder="Optional TITAN link"/><button className="button">Send</button></form>}</div></>}
