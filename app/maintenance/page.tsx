import {db} from "@/lib/db";import {money} from "@/lib/money";import ConfirmDelete from "@/components/ConfirmDelete";import {createMaintenanceAction,updateMaintenanceAction,deleteMaintenanceAction} from "../enterprise-actions";
export default async function Page(){const rows=await db.maintenanceRecord.findMany({orderBy:[{status:"asc"},{scheduledAt:"asc"}]});return <><h1>Preventive Maintenance</h1><p className="muted">Schedule printer service, track operating hours and protect uptime.</p><div className="two"><section className="card"><h2>Maintenance register</h2>{rows.map(x=><details className="record" key={x.id}><summary><b>{x.assetName}</b> <span className="muted">{x.maintenanceType} · {x.status} · {money(x.costCents)}</span></summary><form action={updateMaintenanceAction} className="form editForm"><input type="hidden" name="id" value={x.id}/><select name="status" defaultValue={x.status}><option>SCHEDULED</option><option>IN_PROGRESS</option><option>COMPLETED</option><option>DEFERRED</option><option>CANCELLED</option></select><input name="scheduledAt" type="date" defaultValue={x.scheduledAt?.toISOString().slice(0,10)||""}/><input name="meterHours" type="number" step="0.1" defaultValue={x.meterHours??""}/><input name="cost" type="number" step="0.01" defaultValue={(x.costCents/100).toFixed(2)}/><input name="performedBy" defaultValue={x.performedBy||""}/><textarea name="notes" defaultValue={x.notes||""}/><div className="actions"><button className="button small">Save</button><ConfirmDelete action={deleteMaintenanceAction} id={x.id}/></div></form></details>)}</section><form action={createMaintenanceAction} className="card form"><h2>Schedule maintenance</h2><select name="assetType"><option>PRINTER</option><option>DRYER</option><option>WASH_CURE</option><option>SCANNER</option><option>OTHER</option></select><input name="assetName" required placeholder="Bambu X1C #1"/><input name="assetId" placeholder="Asset record ID (optional)"/><input name="maintenanceType" required placeholder="Nozzle replacement / lubrication"/><input name="scheduledAt" type="date"/><input name="meterHours" type="number" step="0.1" placeholder="Meter hours"/><input name="cost" type="number" step="0.01" placeholder="Estimated cost"/><input name="performedBy" placeholder="Technician"/><textarea name="notes"/><button className="button">Schedule</button></form></div></>}
