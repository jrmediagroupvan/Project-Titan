import {db} from "@/lib/db"; export const dynamic="force-dynamic";
export default async function Activity(){const events=await db.auditEvent.findMany({take:250,orderBy:{createdAt:"desc"},include:{user:true}});return <><h1>Activity Log</h1><section className="card"><table className="table"><thead><tr><th>Time</th><th>User</th><th>Action</th><th>Summary</th></tr></thead><tbody>{events.map(e=><tr key={e.id}><td>{e.createdAt.toLocaleString('en-CA')}</td><td>{e.user?.name||'System'}</td><td>{e.action}</td><td>{e.summary}</td></tr>)}{!events.length&&<tr><td colSpan={4} className="muted">New audited actions will appear here.</td></tr>}</tbody></table></section></>}
