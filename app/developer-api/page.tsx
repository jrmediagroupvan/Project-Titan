import {db} from "@/lib/db";import ConfirmDelete from "@/components/ConfirmDelete";import {createApiCredentialAction,revokeApiCredentialAction,createWebhookAction,toggleWebhookAction,deleteWebhookAction} from "../enterprise-actions";
export default async function Page(){const [keys,hooks]=await Promise.all([db.apiCredential.findMany({orderBy:{createdAt:"desc"}}),db.webhookEndpoint.findMany({orderBy:{createdAt:"desc"}})]);return <><h1>Developer API & Webhooks</h1><p className="muted">Issue scoped machine credentials and deliver business events to approved HTTPS endpoints.</p><div className="two"><section className="card"><h2>API credentials</h2>{keys.map(k=><article className="record" key={k.id}><b>{k.name}</b><p className="muted">{k.keyPrefix}… · {k.active?"Active":"Revoked"} · {k.scopes.join(", ")}</p>{k.active&&<form action={revokeApiCredentialAction}><input type="hidden" name="id" value={k.id}/><button className="secondary small">Revoke</button></form>}</article>)}</section><form action={createApiCredentialAction} className="card form"><h2>Create API credential</h2><input name="name" required placeholder="Shop floor integration"/><input name="scopes" required placeholder="customers:read, orders:write"/><input name="secret" minLength={24} placeholder="Optional API secret (24+ characters); leave blank to generate"/><input name="expiresAt" type="date"/><p className="muted">When supplying your own secret, store it safely before submitting. TITAN encrypts it and displays only its prefix afterward.</p><button className="button">Create credential</button></form></div><div className="two"><section className="card"><h2>Webhook endpoints</h2>{hooks.map(w=><article className="record" key={w.id}><b>{w.name}</b><p className="muted">{w.endpointUrl} · {w.active?"Enabled":"Paused"} · {w.events.join(", ")}</p><div className="actions"><form action={toggleWebhookAction}><input type="hidden" name="id" value={w.id}/><button className="secondary small">{w.active?"Pause":"Enable"}</button></form><ConfirmDelete action={deleteWebhookAction} id={w.id}/></div></article>)}</section><form action={createWebhookAction} className="card form"><h2>Add webhook</h2><input name="name" required/><input name="endpointUrl" type="url" required placeholder="https://..."/><input name="events" required placeholder="quote.approved, order.paid, print.completed"/><input name="secret" placeholder="Optional signing secret"/><button className="button">Add endpoint</button></form></div></>}
