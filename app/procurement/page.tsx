import {db} from "@/lib/db";import {money} from "@/lib/money";import {createSupplierAction,updateSupplierAction,deleteSupplierAction,createPurchaseOrderAction,updatePurchaseOrderAction,deletePurchaseOrderAction} from "../advanced-actions";import ConfirmDelete from "@/components/ConfirmDelete";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;const [suppliers,orders]=await Promise.all([db.supplier.findMany({orderBy:{name:"asc"}}),db.purchaseOrder.findMany({orderBy:{createdAt:"desc"}})]);return <><h1>Procurement & Suppliers</h1><p className="muted">Manage vendors, purchase orders, incoming material and packaging costs.</p>{q.error&&<p className="alert">This supplier is linked to purchase orders.</p>}<div className="two"><section className="card"><h2>Suppliers</h2>{suppliers.map(x=><details className="record" key={x.id}><summary><b>{x.name}</b> <span className="muted">{x.contactName||""} · {x.active?"Active":"Disabled"}</span></summary><form action={updateSupplierAction} className="form editForm"><input type="hidden" name="id" value={x.id}/><input name="name" defaultValue={x.name}/><input name="contactName" defaultValue={x.contactName||""}/><input name="email" defaultValue={x.email||""}/><input name="phone" defaultValue={x.phone||""}/><input name="website" defaultValue={x.website||""}/><textarea name="notes" defaultValue={x.notes||""}/><label className="check"><input type="checkbox" name="active" defaultChecked={x.active}/> Active</label><div className="actions"><button className="button small">Save</button><ConfirmDelete action={deleteSupplierAction} id={x.id}/></div></form></details>)}</section><form action={createSupplierAction} className="card form"><h2>Add supplier</h2><input name="name" placeholder="Supplier name" required/><input name="contactName" placeholder="Contact"/><input name="email" type="email" placeholder="Email"/><input name="phone" placeholder="Phone"/><input name="website" placeholder="Website"/><textarea name="notes" placeholder="Notes"/><button className="button">Add supplier</button></form></div><div className="two"><section className="card"><h2>Purchase orders</h2>{orders.map(o=><details className="record" key={o.id}><summary><b>{o.number}</b> · {o.supplierName} <span className="muted">{money(o.totalCents)} · {o.status}</span></summary><form action={updatePurchaseOrderAction} className="form"><input type="hidden" name="id" value={o.id}/><select name="status" defaultValue={o.status}>{["DRAFT","ORDERED","PARTIAL","RECEIVED","CANCELLED"].map(v=><option key={v}>{v}</option>)}</select><input name="subtotal" type="number" step="0.01" defaultValue={(o.subtotalCents/100).toFixed(2)}/><input name="tax" type="number" step="0.01" defaultValue={(o.taxCents/100).toFixed(2)}/><input name="expectedAt" type="date" defaultValue={o.expectedAt?.toISOString().slice(0,10)||""}/><textarea name="notes" defaultValue={o.notes||""}/><div className="actions"><button className="button small">Save</button><ConfirmDelete action={deletePurchaseOrderAction} id={o.id}/></div></form></details>)}</section><form action={createPurchaseOrderAction} className="card form"><h2>Create purchase order</h2><select name="supplierId" required><option value="">Choose supplier</option>{suppliers.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><select name="status"><option>DRAFT</option><option>ORDERED</option></select><input name="subtotal" type="number" step="0.01" placeholder="Subtotal"/><input name="tax" type="number" step="0.01" placeholder="Tax"/><input name="expectedAt" type="date"/><textarea name="notes" placeholder="Items and notes"/><button className="button">Create PO</button></form></div></>}
